# 관리자 페이지 비밀번호 (설정하지 않으면 기본값 사용)
# VITE_ADMIN_PASSWORD=원하는비밀번호

# Vercel 등 배포 시: 백엔드 API 주소 (끝에 /api 포함, 예: https://your-backend.railway.app/api)
# 로컬 개발 시 비우면 Vite 프록시(/api → localhost:8080) 사용
# VITE_API_BASE=https://your-backend-url.com/api
